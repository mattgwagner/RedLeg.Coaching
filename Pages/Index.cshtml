@page
@model One_on_Ones.IndexModel
@{
}

@foreach(var board in Model.Boards)
{
   <div>
       <h2>@board.Name</h2>
       <ul>
           @foreach(var list in board.Lists)
           {
               <li>@list.Name</li>
           }
       </ul>
   </div>
}
